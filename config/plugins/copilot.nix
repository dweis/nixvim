{
  plugins = {
    copilot-lua = {
      enable = true;
      settings = {
        suggestion.enabled = false;
        panel.enabled = false;
      };
    };
    copilot-chat = {
      enable = true;
    };
    copilot-cmp = {
      enable = true;
    };
  };
}
